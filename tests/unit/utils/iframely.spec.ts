import * as iframely from '../../../src/utils/iframely'

describe('utils/iframely.ts', () => {
  describe(`.isYouTubeVideoURL`, () => {
    it('WIP', () => {
      expect(true).toBe(true)
    })
  })
})
